services:
  cancellation-predictor:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./outputs:/app/outputs
    networks:
      - bridge

networks:
  bridge:
    driver: bridge
